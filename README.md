#  Синицын Антон Григорьевич ЭФМО-01-25 Практическая работа 7



---

## 🚀 Цель проекта

Разработка Go-приложения с использованием Redis для хранения данных в памяти. Реализован базовый кэш с поддержкой команд **SET**, **GET** и управлением временем жизни ключей (**TTL**).

**Задачи:**

* Изучить базовые операции Redis в Go.
* Настроить кэширование данных для ускорения работы API.
* Понять механизм TTL и его применение.
* Снизить нагрузку на основную базу данных.

---

## 🛠 Технические требования

* **Язык:** Go 1.25+
* **База данных:** Redis
* **Контроль версий:** Git

**Версия Go:**

<img width="317" height="55" alt="image" src="https://github.com/user-attachments/assets/43f9087b-95b9-4c7d-86e9-746258c45c63" />

---

## 📂 Структура проекта

```
pz7-gorm/
├── internal/
│   └── cache/
│       └── cache.go       # Функции кэша
├── cmd/
│   └── server/
│       └── main.go        # Основной сервер
├── go.mod                 # Модуль Go
└── go.sum                 # Контроль зависимостей
```

---

## 📖 О Redis

Redis — это высокопроизводительная **in-memory база данных** с хранением данных по принципу «ключ–значение».

* Доступ к данным за миллисекунды.
* Используется для кэширования, брокера сообщений, счётчиков и хранения сессий.

**TTL (Time-to-Live)** — механизм автоматического удаления ключей после заданного времени:

* Позволяет поддерживать актуальность данных.
* Предохраняет память от переполнения.

---

## 💻 Демонстрация работы

**Запрос `SET`:**

<img width="1369" height="569" alt="image" src="https://github.com/user-attachments/assets/7229d60b-322c-4ee8-aa7c-c81b4459ed84" />

**Запрос `GET`:**

<img width="1373" height="552" alt="image" src="https://github.com/user-attachments/assets/faf16a48-cfd9-48fd-a86f-2df915dbc93a" />

**Запрос `TTL`:**

<img width="1373" height="530" alt="image" src="https://github.com/user-attachments/assets/2252ce79-239f-46a7-ac5c-98e17064b604" />

---

## ❓ Контрольные вопросы

**1. В чём отличие Redis от реляционной БД?**

* Redis хранит данные в памяти и работает с ключами без SQL. Подходит для кэша и быстрых операций.
* Реляционная БД хранит данные на диске, поддерживает сложные запросы и обеспечивает долговременное хранение.
* Redis быстрее, но данные могут теряться при перезагрузке.

**2. Какие типы данных поддерживает Redis кроме строк?**

* Списки, множества, хэши, упорядоченные множества, битовые поля, гео-данные.
* Для каждого типа предусмотрены специальные команды.

**3. Зачем нужен TTL? Примеры использования:**

* Автоматическое удаление устаревших данных.
* Примеры:

  * Популярные товары: TTL 5 мин → обновление каждые 5 минут.
  * Сессии пользователей: TTL 1 час → удаление неактивных пользователей.

**4. Как Redis снижает нагрузку на PostgreSQL?**

* Первый запрос: проверка Redis → если данных нет → запрос к PostgreSQL → запись в Redis с TTL.
* Последующие запросы используют данные из памяти, уменьшая нагрузку на БД.

**5. Что происходит, если ключ устарел по TTL?**

* GET возвращает ошибку `redis.Nil`.
* Приложение обращается к БД, получает свежие данные, сохраняет обратно в Redis с новым TTL.

---

## 🎯 Выводы

* Освоены основные операции Redis в Go.
* Реализован кэш с TTL для ускорения API.
* Показана эффективность Redis для снижения нагрузки на реляционную БД.
* Механизм TTL обеспечивает актуальность данных и контроль памяти.
